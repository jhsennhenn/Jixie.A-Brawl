{
    "Name": "Jixie.A-Brawl",
    "Description": "Replaces the A-Wall with a handheld dome shield (Titan bubble shield model, rotated forward) carried in front of the player.",
    "Version": "0.4.0",
    "LoadPriority": 1,

    "ConVars": [
        {
            "Name": "a_brawl_shield_health",
            "DefaultValue": "400",
            "Description": "HP of the held shield before it breaks. 0 = use default (850)."
        },
        {
            "Name": "a_brawl_shield_forward_offset",
            "DefaultValue": "50",
            "Description": "Distance in front of the player the dome hovers (units). The xo_shield model is large so this needs to be higher than the flat-wall version."
        },
        {
            "Name": "a_brawl_shield_vertical_offset",
            "DefaultValue": "0",
            "Description": "Vertical offset of the dome (positive = up, negative = down)."
        },
        {
            "Name": "a_brawl_shield_duration",
            "DefaultValue": "10",
            "Description": "Shield lifetime in seconds. 0 = infinite."
        },
        {
            "Name": "a_brawl_dome_scale",
            "DefaultValue": "0.35",
            "Description": "Scale passed to the bubble shield FX via CP2. The xo_shield model is Titan-scale; 0.35 brings it down to roughly pilot size. Affects the FX bubble only, not the collision geometry."
        }
    ],

    "Scripts": [
        {
            "RunOn": "SERVER",
            "ServerCallback": {
                "Init": "MpWeaponDeployableCover_Init"
            }
        }
    ]
}
